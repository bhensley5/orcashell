[package]
name = "orcashell-syntax"
version = "0.3.2"
edition.workspace = true

[dependencies]
syntect = { version = "5", default-features = false, features = ["default-syntaxes", "regex-onig", "yaml-load"] }
orcashell-store = { path = "../orcashell-store" }
